export interface CoreIconProps {
	/** Iconify icon id exactly as documented (e.g. `material-symbols:home-rounded`, `bx:home`). */
	icon?: string | null;
	style?: string;
	class?: string;
	width?: string | number;
	height?: string | number;
}
